# LAMBDA FUNCTION :The function which dontt have name and also use for return or print some thing only once called as lambda function .
# Qn=>>Create a lambda function to compute the cube of a number.

cube = lambda x: x**3

print(cube(4))